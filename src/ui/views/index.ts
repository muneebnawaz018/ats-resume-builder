/**
 * One view per route.
 *
 * Next requires every route file to be called `page.tsx`, which makes a folder
 * of routes unreadable in an editor tab bar. The route files stay as thin
 * metadata declarations and the page itself lives here under its own name.
 */
export { CheckerView } from "./CheckerView";
export { HomeView } from "./HomeView";
export { LegalView } from "./LegalView";
export { NotFoundView } from "./NotFoundView";
export { PrivacyContent } from "./PrivacyContent";
export { TermsContent } from "./TermsContent";
