/**
 * The resume checker console.
 *
 * Self-contained: its own stylesheet, its own dark token scope, and its own
 * copies of the picker and the saved-document list. It shares nothing with
 * site.module.css beyond the chrome that PageShell renders around it.
 */
export { CheckerConsole } from "./CheckerConsole";
